我目前有两个api的接口文档。
1. 用户登录接口（登录成功后，返回authorcode用于后续接口进行权限验证）
2. 信息查询接口，接收“标题关键词”“全文关键词"等，返回结果目录。

请将上面两个接口改为mcp服务。之后给出如何使用的调用例子。给出代码例子和文档说明。





任务1：

1. 请理解”C:\gzb_file_to_github\P13_faxin_mcp\0_接口文档\1_法信用户登录接口.md" 文档，编写调用此API接口的，调用程序。
封装成一个比较容易调用的类文件。
2. 之后编写对一个的MCP服务器端程序，提供MCP访问，方便远程调用。
3. 上面的程序代码放到：C:\ai_project\59_faxin_mcp_ai\3_MCP_Server\ 目录下。

4. 编写调用上面 MCP服务的客户端程序，让我进行服务的调用测试。（使用本地的ollama 中的模型：qwen3.5作为，本地agent的模型）（支持使用调用 阿里千问的模型api接口调用）,程序代码放在：C:\gzb_file_to_github\P13_faxin_mcp\4_MCP_Client\ 目录下。
